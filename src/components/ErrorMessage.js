import React, { useEffect } from 'react';

const ErrorMessage = props => {
  useEffect(() => {});
  return (
    <div>
      <p className={props.class}>Keep Looking!</p>
    </div>
  );
};

export default ErrorMessage;
